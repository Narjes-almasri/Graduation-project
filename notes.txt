1. login/sign up:
 • if a user signs up and then goes to the login page they must enter the exact pass they created during sign up to log in successfully
 • users can only log in with an email address that has already been registered in the system
 • entering a random pass or unregistered email should fail and prevent login
 • if users forget their password they can use the forgot password option to reset it
 • users can also log in using their google or apple accounts
 • if the user logs in and makes changes like adding a profile picture or changing their username etc.. and then logs out and back in all their data should remain saved and NOT return to its default state their changes should be saved
 اشتغل بدون سب عاليوزر لو سمحت 
///////
2.Profile Setup (profile_setup.html):
• users can fill in or update their profile information including first name last name website/business name and phone number
• when the form is revisited previously saved information should automatically appear

3.Color Palette Selection (color_palette.html):
• users can choose a color palette for their website from predefined options or from an uploaded logo
• when the user clicks the “Continue” btn the selected palette should be saved and applied to the website in the upcoming pages.

4.App Setup (app_setup.html):
• users can upload a logo for their brand the logo should be saved on the server
• the brand name entered by the user should be stored and persist for future pages
• the stored logo and brand name should be available for downstream pages such as (final preview ,build_preview.html, generation(only the brand name))

5. Logo Generation (generation.html):
• users can generate a logo (it will be PNG from html2canvas) and upload it to the server
• the saved logo should be made available to later pages such as build preview and final preview

6.Build Preview (build_preview.html):
• load all user related content: profile info, color palette, logo, text content, and uploaded images
• users can edit content for all sections (hero, feature, carousel, products, quote, team), and changes should persist
• clicking “Save Work” should create or update a website snapshot on the server
• users can request an admin evaluation; the system should generate like a request ID and track its status on the admin dashboard
• when a website is finished, the system should automatically add an icon for that website to the user’s profile.

7. Final Preview (final_preview.html):
• load a saved website 
• serve all saved content: palette, logo (with positioning and zoom), text content, and images for read only viewing

8. Admin Dashboard (admin_dashboard.html):
• display a list of evaluation requests and saved websites pulled from the backend

///
2. in the build_preview.html page :
 • when the user clicks on the Admin Evaluation button the request should be reflected on the admin_dashboard page (if it's hard it's ok to skip it)

 ------ also when the user finishes their website i want the icon of the website to be added to their profile ----- (bardo if it's hard for u it's ok to skip it cuz it's just about details)
 
